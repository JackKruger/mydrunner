// Build-time constants injected by Vite's `define`. See vite.config.ts.

/** Version string baked at build time. Format: "<commit-count>.<short-sha>"
 *  for deployed builds (e.g. "347.8bc26f1"); the literal "dev" for local
 *  dev runs where APP_VERSION isn't set. Rendered in the bottom-right
 *  corner of the page. */
declare const __APP_VERSION__: string;
